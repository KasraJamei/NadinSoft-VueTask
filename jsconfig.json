{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    // استفاده از bundler برای سازگاری بهتر با Vite
    "moduleResolution": "bundler", 
    "allowJs": true,
    // **۱. حل خطای JSON:** به کامپایلر اجازه می‌دهد فایل‌های .json را وارد کند.
    "resolveJsonModule": true, 
    "esModuleInterop": true, 
    // ۲. Alias @
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  // اطمینان از اینکه فایل‌های JSON هم در بررسی TypeScript/LSP لحاظ می‌شوند.
  "include": [
    "src/**/*.js", 
    "src/**/*.ts", 
    "src/**/*.vue",
    "src/**/*.json"
  ], 
  "exclude": ["node_modules", "dist"]
}